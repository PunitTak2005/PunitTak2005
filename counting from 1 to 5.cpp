#include <stdio.h>

int main() {
    int count = 1;

    printf("Counting from 1 to 5 using a while loop:\n");

    while (count <= 5) {
        printf("%d\n", count);
        count++;
    }

    return 0;
}
